# Environment variables used when running
# the font-end locally and the back-end remotely

API_BASE_URL="https://oruow58cxd.execute-api.us-east-2.amazonaws.com/Prod/"

COGNITO_DOMAIN="tp-capstone.auth.us-east-2.amazoncognito.com"
COGNITO_USER_POOL_ID="us-east-2_XInZ9uvvy"
COGNITO_USER_POOL_CLIENT_ID="3njhraq00fdn5oq98ik96ge0q3"

COGNITO_REDIRECT_SIGNIN="http://localhost:8000"
COGNITO_REDIRECT_SIGNOUT="http://localhost:8000"
